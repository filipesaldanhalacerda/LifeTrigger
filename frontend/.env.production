# Production: Vercel rewrites proxy /api/auth/* and /api/engine/* to the deployed APIs
# These stay as relative paths — Vercel rewrites handle the rest
VITE_AUTH_API_URL=/api/auth
VITE_ENGINE_API_URL=/api/engine
